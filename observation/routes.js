import Database from "../Database/index.js";

function oberveDetailRoute(app) {
    // // populating all observation
    // app.get("/project/observations", async (req, res) => {
    //     const observations = Database.observations;
    //     res.json(observations);
    // });
    // // getting one observation.
    // app.get("/project/observations/:id", async (req, res) => {
    //     const id = req.params.id;
    //     const observation = Database.observations.find((observation) => observation.observation_id === id);
    //     res.json(observation);
    // });



    
}

export default oberveDetailRoute;